spring.application.name=account-service

# default port 8080, loan-service will use 8081 since both can't share the same port
server.port=8080

# points at the eureka discovery server so this service can register itself
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
